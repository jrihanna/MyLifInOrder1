package com.example.mylifeinorder1.activity;

import android.text.InputType;
import android.widget.EditText;
import android.widget.LinearLayout;

public class LoanActivity { // extends HistoryActivity

   /* @Override
    public LinearLayout createLayout() {
        LinearLayout.LayoutParams params = new LinearLayout.LayoutParams(
                LinearLayout.LayoutParams.MATCH_PARENT,
                LinearLayout.LayoutParams.WRAP_CONTENT
        );
        params.setMargins(10,0,0,0);

        LinearLayout c = new LinearLayout(this);
        c.setOrientation(LinearLayout.VERTICAL);

        EditText loanType = new EditText(this);
        loanType.setLayoutParams(params);
        loanType.setHint("Loan Type");

        EditText instituteName = new EditText(this);
        instituteName.setLayoutParams(params);
        instituteName.setHint("Owed To");

        EditText amount = new EditText(this);
        amount.setInputType(InputType.TYPE_CLASS_NUMBER);
        amount.setLayoutParams(params);
        amount.setHint("Amount");

        c.addView(loanType);
        c.addView(instituteName);
        c.addView(amount);

        return c;
    }*/
}
